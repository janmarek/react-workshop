# React workshop

## Installation

Gulp will be needed:

`npm install -g gulp`

Install project dependencies:

`npm install`

Run local server:

`gulp dev-server`

open [http://localhost:4000](http://localhost:4000)
